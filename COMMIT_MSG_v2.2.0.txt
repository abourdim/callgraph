feat: Callgraph Studio v2.2.0 — Data Dependencies report

New: 📊 Data button generates standalone HTML with:
- Global variable data flow (writer→reader chains with SVG diagrams)
- Peripheral register map (per-register, ISR vs task, conflict flags)
- RTOS queue/mutex dependencies (producer→consumer with SVG)
- Struct field tracking (which functions touch which HW register fields)
- Sidebar TOC, scroll spy, search, expand/collapse, dark/light theme
- Per-item flow diagrams (writers → variable/object → readers)
- Conflict detection (ISR/task concurrent access flagged)
- Cross-module data sharing highlighted

Tests: 794 · Lines: 14,000+
